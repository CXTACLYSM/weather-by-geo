## weather-by-geo

Practice telegram-bot project with versions:
1. `v1` - using only net/http with custom structs.
2. `v2` - using telegram library.

How to run: go to `v1` or `v2` folder to see corresponding `README.md`.
